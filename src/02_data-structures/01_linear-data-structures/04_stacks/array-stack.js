const stack_push_pop = [];

stack_push_pop.push('google');
stack_push_pop.push('facebook');
stack_push_pop.push('twitter');
console.log(stack_push_pop);

stack_push_pop.pop();
console.log(stack_push_pop);

stack_push_pop.pop();
console.log(stack_push_pop);

const stack_unshift_shift = [];

stack_unshift_shift.unshift('google');
stack_unshift_shift.unshift('facebook');
stack_unshift_shift.unshift('twitter');
console.log(stack_unshift_shift);

stack_unshift_shift.shift();
console.log(stack_unshift_shift);

stack_unshift_shift.shift();
console.log(stack_unshift_shift);
