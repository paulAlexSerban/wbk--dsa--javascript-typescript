export const reverseString = (str: string): string => {
    // Split the string into an array of characters, reverse that array, and then join it back into a string
    return str.split('').reverse().join(''); // reverse this!
};
