/**
 * Core JS - Null
 * ===============
 */

// Null is a special type that represents the absence of a value.
// It is a primitive value that represents the intentional absence of a value, and is manipulated as any other primitive value.
// It is one of JavaScript's primitive values and is treated as falsy for boolean operations.

(() => {
  const a = null;
  console.log(a);
})()